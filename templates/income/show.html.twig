{% extends 'base.html.twig' %}

{% block title %}Income{% endblock %}

{% block body %}
    <h1>Income</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Amount</th>
                <td>{{ income.amount }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ income.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ income.title }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ income.createdAt ? income.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ income.updatedAt ? income.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_income_index') }}">back to list</a>

    <a href="{{ path('app_income_edit', {'id': income.id}) }}">edit</a>

    {{ include('income/_delete_form.html.twig') }}
{% endblock %}
